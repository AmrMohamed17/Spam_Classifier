{
  "version": 2,
  "builds": [
    {
      "src": "app.py",
      "use": "@vercel/python",
      "config": {
        "maxLambdaSize": "50mb",
        "memory": 1024
      }
    }
  ],
  "scripts": {
    "build": "pip install --no-cache-dir --no-deps -r requirements.txt"
  }
}
